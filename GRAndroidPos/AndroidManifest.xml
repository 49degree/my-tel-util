<?xml version="1.0" encoding="utf-8"?>                                                                     
<manifest xmlns:android="http://schemas.android.com/apk/res/android"                     
	package="com.guanri.android.jpos" android:versionCode="1"
	android:versionName="1.0">
	
	<application android:name="com.guanri.android.lib.context.MainApplication"
	   android:icon="@drawable/icon" android:label="@string/app_name">
		<activity android:name=".MainActivity" android:launchMode="singleInstance"
		 android:label="@string/app_name">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name="Main" android:label="@string/app_name">

		</activity>
		<!-- 开机接收广播-->
		<receiver android:name=".broadcast.BootBroadcastReceiver">
			<intent-filter>
				<action android:name="android.intent.action.BOOT_COMPLETED" />
			</intent-filter>
		</receiver>
		<!-- 关机接收广播 -->
		<receiver android:name=".broadcast.ShutdownReceiver">
			<intent-filter>
				<action android:name="android.intent.action.ACTION_SHUTDOWN" />
			</intent-filter>
		</receiver>
		<!-- 自动运行服务 -->
		<service android:enabled="true" android:name=".services.AutoRunService"></service>
		<!-- 提供其他服务调用服务 -->
		<service android:name=".services.AidlRunService" android:process=":remote">
			<intent-filter>
				<action android:name="com.guanri.android.jpos.services.GrPosService" />
			</intent-filter>
		</service>
	</application>
    <uses-sdk android:minSdkVersion="3" />
	<!-- 接受系统启动完毕的 Broadcast 的权限 -->
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
	<uses-permission android:name="android.permission.INTERNET"></uses-permission>
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	
</manifest>